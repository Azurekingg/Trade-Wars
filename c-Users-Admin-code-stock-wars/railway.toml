[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn -w 4 -b 0.0.0.0:$PORT app:app"
restartPolicyType = "always"
restartPolicyMaxRetries = 5
restartPolicyType = "always"
restartPolicyMaxRetries = 5
